[phases.setup]
nixPkgs = ["python313", "zbar", "poppler_utils", "postgresql"]

[phases.build]
cmds = ["echo 'Build complete'"]

[start]
cmd = "cd backend && LD_LIBRARY_PATH=/root/.nix-profile/lib:$LD_LIBRARY_PATH python -m uvicorn main:app --host 0.0.0.0 --port ${PORT:-8000}"
